# Required for real AI-generated lessons/quizzes/roadmaps.
# Without this key, every tool still works using the built-in
# deterministic fallback generators (no server crash, just less
# dynamic content).
GEMINI_API_KEY=

NODE_ENV=production
