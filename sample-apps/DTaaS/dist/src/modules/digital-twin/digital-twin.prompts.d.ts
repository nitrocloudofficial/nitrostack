import { ExecutionContext } from "@nitrostack/core";
export declare class DigitalTwinPrompts {
    getSmartHomePrompt(args: Record<string, never>, ctx: ExecutionContext): Promise<{
        role: "user";
        content: string;
    }[]>;
    getSmartFactoryPrompt(args: Record<string, never>, ctx: ExecutionContext): Promise<{
        role: "user";
        content: string;
    }[]>;
    getSmartHospitalPrompt(args: Record<string, never>, ctx: ExecutionContext): Promise<{
        role: "user";
        content: string;
    }[]>;
    getSmartWarehousePrompt(args: Record<string, never>, ctx: ExecutionContext): Promise<{
        role: "user";
        content: string;
    }[]>;
}
//# sourceMappingURL=digital-twin.prompts.d.ts.map