# NitroStack Configuration
NITRO_LOG_LEVEL=info
NITROSTACK_APP_MODE=openai

# Server Transport Configuration (Optional)
# =============================================================================
# MCP_TRANSPORT_TYPE: Toggles transport mode. Values: stdio | http | dual.
# Defaults to 'stdio' in development and 'dual' in production/NODE_ENV=production.
# =============================================================================
# MCP_TRANSPORT_TYPE=stdio
# PORT=3000
# HOST=localhost
# ENABLE_CORS=true

# MongoDB Atlas — persistent findings pipeline (findings/finding_events collections,
# database "warden"). Network Access on the Atlas project must allow this server's
# egress IP (or 0.0.0.0/0 for PaaS hosts with no fixed IP).
MONGODB_URI=
