# NitroForge environment variables — copy to .env, never commit real values

NODE_ENV=development

# Required by W1's planner.service.ts (the only LLM call in the system)
ANTHROPIC_API_KEY=

# Server
PORT=3000
