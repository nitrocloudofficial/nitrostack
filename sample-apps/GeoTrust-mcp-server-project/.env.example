# GeoTrust AI — MCP Server Configuration

# NitroStack Server
NITRO_LOG_LEVEL=info
NITROSTACK_APP_MODE=universal

# ─── NVIDIA NIM (required for Chef script) ───────────────────────────────────
# Get your free API key at: https://build.nvidia.com
# Model used: meta/llama-3.3-70b-instruct (supports tool calling)
NVIDIA_API_KEY=nvapi-your_key_here

# Optional: override the NIM model
# NIM_MODEL=meta/llama-3.3-70b-instruct

# ─── Server Transport ────────────────────────────────────────────────────────
# MCP_TRANSPORT_TYPE: stdio | http | dual
#   - stdio: local NitroStudio development (default for `npm run dev`)
#   - http:  cloud deployment (HTTP + SSE for remote MCP clients)
#   - dual:  both transports (recommended for production)
MCP_TRANSPORT_TYPE=dual
PORT=3000
HOST=0.0.0.0
ENABLE_CORS=true

# ─── CORS Origins (optional, comma-separated) ────────────────────────────────
# CORS_ORIGINS=https://your-domain.com,https://studio.nitrostack.ai
