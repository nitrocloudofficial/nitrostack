'use client';import type{ReactNode}from'react';import{statusColor}from'../../utils/formatters';
export const Panel=({title,children,status}:{title:string;children:ReactNode;status?:string})=><section style={{background:'#fff',border:'1px solid #e2e8f0',borderRadius:14,padding:18,minWidth:0}}><header style={{display:'flex',justifyContent:'space-between',gap:8,alignItems:'center'}}><h2 style={{fontSize:18,margin:0}}>{title}</h2>{status&&<span aria-label={`Status ${status}`} style={{color:statusColor(status),fontWeight:700}}>{status}</span>}</header><div style={{marginTop:14}}>{children}</div></section>;
export const Empty=({children}:{children:ReactNode})=><p style={{color:'#64748b'}}>{children}</p>;export const Metric=({label,value}:{label:string;value:ReactNode})=><div><small style={{color:'#64748b'}}>{label}</small><strong style={{display:'block',marginTop:3}}>{value}</strong></div>;
